# Changelog

同窓会ホームページパッケージ（Alumni Core / Alumni Theme）のリリース記録。

## [0.2.0] - 2026-08-28

基盤構築フェーズ（0.1.0）に続き、ニュース／イベント機能と学校ブランディング／写真機能を追加したリリース。

### 変更内容

**ニュース／イベント**
- 専用Custom Post Type（`alumni_news_event`）によるニュース／イベント共通コンテンツ
- タイトル・内容・種別（ニュース／イベント）・開催日（イベントのみ）を分離した専用入力フォーム。Gutenberg／クラシックエディタは無効化し、自由なブロック編集ではなく固定項目の入力に統一
- イベントの開催日をタイトルより先に表示（トップページ・一覧ページ・詳細ページ共通）
- 開催日未入力のイベントは公開できず、下書き化＋警告表示

**学校ブランディング**
- 学校の校章・同窓会独自ロゴのアップロード／メディアライブラリからの選択（基本設定画面）
- 校章・ロゴは別々の項目として管理し、どちらも任意。ヘッダーに校章→同窓会名→ロゴの順で表示（未設定時は領域ごと非表示）

**学校関連写真**
- 複数写真の登録・削除・並び替え（「同窓会」→「学校写真」画面）
- 表示方式（固定表示／自動切替）の選択、固定表示時の表示画像選択
- トップページでの写真表示（固定表示は1枚を大きく、自動切替は約5秒ごとにフェード切替。外部ライブラリ不使用の素のCSS/JS）
- 登録済み画像がメディアライブラリから削除された場合、実在する画像のみを対象に自動的にフォールバック（固定表示の指定画像が削除済みなら有効な先頭画像へ、有効な写真が0枚なら何も表示しない）

**学校創立年**
- 未設定サイトのみ初期値を1950年に変更。既存サイトの保存済み設定値（空文字を含む）は一切上書きしない

**基盤の改善**
- 複数の設定画面（基本設定／学校写真）が同じ設定オプション行を安全に分担保存できるよう、`Settings::update_field()`/`update_fields()` および現在値へのフォールバックによる保存ロジックを整理
- Attachment IDの検証（実在確認・画像形式確認）を保存時・表示時で共通のヘルパーに統一

### テスト状況

- `php -l` を全PHPファイルに対して実行し、構文エラーがないことを確認
- 既存のスタンドアロンテスト（`Term_Calculator`、`Settings`/`Installer`、ニュース／イベント関連）を再実行し回帰なし
- 新規スタンドアロンテスト：ニュース／イベントの入力フォーム・必須項目バリデーション、学校写真の保存時／表示時の検証ロジック（有効・無効・削除済みAttachment IDの扱い、固定表示のフォールバック、0/1/複数枚の表示）、校章・ロゴの設定保持（他フォームの保存で意図せず消えないこと）、管理画面アセットの読み込み範囲
- **未実施**：実際のWordPress管理画面でのメディアピッカー操作、ギャラリー並び替え、公開側の視覚的表示、スライドショーの動作確認。nisehatakiti.online での実機確認で対応予定

### 導入手順（nisehatakiti.online）

1. WordPress管理画面にログイン
2. 「プラグイン」→「新規追加」→「プラグインのアップロード」から `alumni-core.zip` をアップロード（既存プラグインを更新する場合は、既存のAlumni Coreを無効化・削除してから同じ手順でアップロードするか、FTP/SSH等で `wp-content/plugins/alumni-core/` を直接上書きする）
3. Alumni Core を有効化
4. 「外観」→「テーマ」→「新規追加」→「テーマのアップロード」から `alumni-theme.zip` をアップロード（既存テーマは上書きされる）
5. Alumni Theme を有効化
6. 管理画面の「同窓会」→「基本設定」「学校写真」を開き、必要な情報・画像を登録
7. `/news-events/` へのアクセスが404になる場合は、「設定」→「パーマリンク設定」で変更を保存してリライトルールを再生成

ZIPは `scripts/build-release-zips.sh` を実行することで `dist/alumni-core.zip` / `dist/alumni-theme.zip` として再生成できます（開発用ファイルを含まない、WordPressにそのままアップロード可能な構成）。

## [0.1.0] - 2026-08-28

初回リリース。実装指示書①（同窓会ホームページパッケージ 基盤構築）に基づく基盤構築フェーズ。

### 変更内容

**Alumni Core（プラグイン）**
- プラグイン本体・起動処理、有効化/無効化/インストーラー
- 「同窓会」管理メニューとダッシュボード／基本設定。将来のサブメニュー追加はフックで拡張可能
- 同窓会基本設定：同窓会名称・学校名称・学校創立年（任意）・第1期卒業年・卒業期カラー機能（ON/OFF、可変長カラー周期）
- 卒業年→卒業期、卒業期→カラーの共通計算ロジック（`Term_Calculator`）
- 設定フォームの nonce 検証・権限チェック・入力サニタイズ・出力エスケープ
- プラグイン更新時にもDBバージョン差分を検知してアップグレード処理を実行する仕組み
- 破損した設定値（`colors`が配列でない場合等）に対する安全側フォールバック

**Alumni Theme（テーマ）**
- ヘッダー／ナビゲーション／メインコンテンツ／フッターが動作するシンプルな構成
- Alumni Core が無効な場合でも致命的エラーにならず、通常のサイト名等にフォールバックして動作

**未実装（今後のフェーズ）**
- 沿革・校歌・組織図・役員一覧、名簿管理、Alumni Voices、Teacher's Voice、メールマガジン、レイアウトビルダー

### テスト状況

- `php -l` を全PHPファイルに対して実行し、構文エラーがないことを確認
- `Term_Calculator` の単体ロジック（卒業期計算・カラー周期計算・境界値）をスタンドアロンのテストスクリプトで検証
- WordPress関数の最小スタブを用いた `Settings` / `Installer` のロジック検証（年バリデーション、DBバージョンアップグレードの分岐、`colors`破損時の自動修復）
- **未実施**：実際のWordPress/MySQL環境での有効化・管理画面表示・設定保存の動作確認
